<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
			"-//mybatis.org//DTD Mapper 3.0//EN" 
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
			
<mapper namespace="com.itbank.dao.OrderDAO">
	
	<select id="selectStatus" parameterType="hashmap" resultType="order">
		select * from service 
		<if test="!status.equals('')">
			where service_status = #{status}
		</if>
		<if test="status.equals('')">
			where service_status is not null
		</if> 
		<if test="type != null">
			<if test="type.equals('title')">
				and service_title like '%${search}%'
			</if>
			<if test="type.equals('content')">
				and service_content like '%${search}%'
			</if>
			<if test="type.equals('both')">
				and service_title like '%${search}%' or service_content like '%${search}%'
			</if>
		order by service_idx desc
		offset ${offset} rows
		fetch first ${nowD} rows only
		</if>
	</select>
	
	<select id="selectBoardCountList" parameterType="hashmap" resultType="int">
		select count(*) from service 
		<if test="!status.equals('')">
			where service_status = #{status}
		</if>
		<if test="status.equals('')">
			where service_status is not null
		</if> 
		<if test="type != null">
			<if test="type.equals('title')">
				and service_title like '%${search}%'
			</if>
			<if test="type.equals('content')">
				and service_content like '%${search}%'
			</if>
			<if test="type.equals('both')">
				and service_title like '%${search}%' or service_content like '%${search}%'
			</if>
		</if>
	</select>
</mapper>




