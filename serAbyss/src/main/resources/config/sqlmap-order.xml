<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
			"-//mybatis.org//DTD Mapper 3.0//EN" 
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
			
<mapper namespace="com.itbank.dao.OrderDAO">
	<select id = "statusListCount" parameterType="hashmap" resultType="int">
		select count(*) from service where 
		<if test="type != 'both'">
			${type} like '%${keyword}%' 
			<if test="service_status != ''">
				and service_status=#{service_status}
			</if>
		</if>
		
		<if test="type == 'both'">
			service_title like '%${keyword}%' or service_content like '%${keyword}%' 
		</if>
		
		<if test='person_check == "y"'>
			and service_engiId='${person_id}'
		</if>
		<if test='person_check == "n"'>
			and service_custId='${person_id}'
		</if>
		<if test='person_check == "r"'>
			and service_compBelong='${person_belong}'
		</if>
		
	</select>
	
	<select id="selectStatus" parameterType="hashmap" resultType="order">
		select * from service 
		where ${type} like '%${keyword}%' 
		<if test="service_status != ''">
			and service_status=#{service_status} 
		</if> 
		
		<if test='person_check == "y"'>
			and service_engiId='${person_id}'
		</if>
		<if test='person_check == "n"'>
			and service_custId='${person_id}'
		</if>
		<if test='person_check == "r"'>
			and service_compBelong='${person_belong}'
		</if>
		
		order by service_idx desc 
	    offset ${offset} rows 
	    fetch first ${nowD} rows only
	</select>
	
	<insert id = "order" parameterType="order">
	insert into service (service_idx, service_custId, service_title, service_content, 
			 service_status, service_address, service_engiId, service_compBelong, service_name, service_phone)
			 values (service_seq.nextval,
			 <if test="service_custId != null"> 
			 #{service_custId}
			 </if>
			 <if test="service_custId == null"> 
			 null
			 </if>
			 , #{service_title}, #{service_content}, 
			 #{service_status}, #{service_address}, #{service_engiId}, 
			 #{service_compBelong}, #{service_name}, #{service_phone})
	</insert>
	
	<insert id = "insertReserve" parameterType="reserve">
		insert into reserve (reserve_idx, reserve_year, reserve_month, reserve_day, reserve_hour, reserve_engiId,  reserve_custId)
			    values(reserve_seq.nextval, #{reserve_year}, #{reserve_month}, #{reserve_day}, #{reserve_hour}, #{reserve_engiId},
			    <if test="reserve_custId != null"> 
			    #{reserve_custId}
			    </if>
			    <if test="reserve_custId == null"> 
			    null
			    </if>
			    )
	</insert>
</mapper>




